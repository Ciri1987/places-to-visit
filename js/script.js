var elem = document.querySelector('.main-carousel');
var flkty = new Flickity(elem, {
    // options
    imagesLoaded: true,
    cellAlign: 'left',
    pageDots: false,
    contain: true,
    hash: true,
});